//tenpai algorithm
function isTenpai(handTiles) {
	// Sort the hand for easier processing
	const sortedHand = handTiles.slice().sort();
	console.log(sortedHand);

	// Helper function to check if a group of tiles forms a "pon" or a "chi"
	function isSet(tiles) {}

	// Check for pairs and try to complete sets

	// If no tenpai found
	return false;
}

//reset btn
function isTenpai(handTiles) {}
